package com.module.notelycompose.notes.ui.theme

import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight

/**
 * iOS implementation of Material Symbols font families.
 * 
 * For iOS, we use the system default font as a fallback until 
 * Material Symbols fonts are properly integrated for iOS.
 */
actual val MaterialSymbolsOutlined: FontFamily = FontFamily.Default

actual val MaterialSymbolsFilled: FontFamily = FontFamily.Default

actual val MaterialSymbolsLarge: FontFamily = FontFamily.Default
