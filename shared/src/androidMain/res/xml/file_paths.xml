<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For files in getExternalFilesDir("Music") -->
    <external-files-path
        name="recordings"
        path="Music/" />

    <!-- Optional: Add other paths if needed -->
    <cache-path
        name="internal_recordings"
        path="." />
</paths>